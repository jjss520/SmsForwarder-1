name: Weekly Build

# 触发器：手动触发
on:
  workflow_dispatch:
    inputs:
      root_sol:
        description: "Build Title"
        required: true
        default: "SmsForwarder"

# ⚠️ 关键：赋予写权限，否则无法创建 Release
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      output: "${{ github.workspace }}/build/app/outputs/apk/release"
    steps:
      # 1. 检出代码
      - uses: actions/checkout@v4
      
      # 2. 删除旧的工作流记录 (保持页面整洁)
      - name: Delete Old Workflows
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 0
          delete_workflow_pattern: 'Weekly Build'
          
      # 3. 设置 JDK 11
      - name: set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '11'
          java-package: jdk
          
      # 4. 获取打包秘钥 (注意仓库地址)
      - name: Checkout Android Keystore
        uses: actions/checkout@v4
        with:
          repository: jjss520/keystore   # ⚠️ 确认这是你的私有仓库
          token: ${{ secrets.TOKEN }}
          path: keystore
          
      # 5. 开始编译
      - name: Build with Gradle
        run: bash ./gradlew assembleRelease
      
      # 6. 解析版本号 (为了给 Release 命名)
      - name: Parse output-metadata.json
        run: |
          metadata_file="${{ env.output }}/output-metadata.json"
          # 只要找到一个 apk 文件即可提取版本信息
          apk=$(jq -r '.elements[0].outputFile' "$metadata_file")
          
          if [[ $apk =~ SmsF_([^_]+)_([^_]+)_(.+)_release.apk ]]; then
              versionName="${BASH_REMATCH[1]}"
              versionCode="${BASH_REMATCH[2]}"
              
              # 写入环境变量
              echo "ver_name=$versionName" >> $GITHUB_ENV
              echo "ver_code=${versionCode: -3}" >> $GITHUB_ENV # 取最后3位
              
              echo "✅ 解析成功: v$versionName ($versionCode)"
          else
              echo "❌ 解析文件名失败，使用默认时间戳作为版本号"
              echo "ver_name=$(date +%Y%m%d)" >> $GITHUB_ENV
              echo "ver_code=$(date +%H%M)" >> $GITHUB_ENV
          fi

      # 7. 发布到 GitHub Releases 🚀
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          # Tag 命名格式：v3.3.3_100054 (这样每次版本号变动都会发新版)
          tag_name: v${{ env.ver_name }}_${{ env.ver_code }}
          name: SmsForwarder v${{ env.ver_name }} (Build ${{ env.ver_code }})
          body: |
            自动化编译构建
            - 新增 打开软件密码功能
            - 编译时间: ${{ steps.date.outputs.date }}
          draft: false
          prerelease: false
          # 使用通配符上传所有 APK
          files: ${{ env.output }}/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }} # 使用你的 TOKEN 才有权限发布

      # 8. (可选) 依然保留 Artifacts 备份，防止 Release 失败没地方下
      - name: Upload Artifacts Backup
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "SmsForwarder_v${{ env.ver_name }}_All_APKs"
          path: "${{ env.output }}/*.apk"

